# Core ML Libraries (PyTorch stack installed by batch script)
diffusers>=0.32.0
transformers>=4.48.0
accelerate>=1.3.0
safetensors>=0.5.0
bitsandbytes>=0.48.2  # Use stable version (0.49.x may not exist)

# Qt Interface
PySide6>=6.7.0
lpips>=0.1.4
pyqtgraph>=0.13.4

# Computer Vision & Processing
opencv-python-headless>=4.10.0.84
Pillow>=11.0.0
tqdm>=4.67.0
numpy>=1.26.0,<2.0
protobuf>=4.25.3,<5.0
psutil>=6.1.0
scipy>=1.15.0
pandas>=2.2.0
matplotlib>=3.10.0
scikit-image

# SD-specific optimizations
tomesd>=0.1.3
peft>=0.14.0

# Windows-specific (Linux uses the Triton supplied by its PyTorch install)
triton-windows>=3.0.0; platform_system == "Windows"
pywin32>=308; platform_system == "Windows"
ultralytics>=8.3.0
